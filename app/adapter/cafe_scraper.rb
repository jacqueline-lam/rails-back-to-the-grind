require 'open-uri'
require 'nokogiri'
require 'pry'

class CafeScraper
  # def scrape_cafes
    # doc = Nokogiri::HTML(open(#URL))
    # attrs_arr = []
    # doc.css().each do ||
    # end
    # writing data to a JSON file in JSON format in case article changes/ deletes data; 
    # no need to scrape it everytime
    # save to file and have it locally and script will be run once
    # File.write('cafes.json', attrs_arr.to_json)
  # end
end